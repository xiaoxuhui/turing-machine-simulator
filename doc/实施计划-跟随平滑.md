# 实施计划：跟随读写头视角平滑移动

按 plan-driven-frontend-dev 规范，分阶段实现、每阶段一 commit、测试驱动。

## 阶段 A：规划文档（本文件 + 需求 + 设计）
- 写 `doc/需求与测试用例-跟随平滑.md`、`doc/设计文档-跟随平滑.md`、`doc/实施计划-跟随平滑.md`。
- 提交：`docs: plan smooth movement for follow-head tape view`

## 阶段 B：core.ts 纯函数 + 单测
- 新增 `headFollowShift(fromCenter, toCenter, pitch)`。
- `tests/core.test.ts` 增加 4 个用例（见需求文档）。
- 跑 `vitest run tests/core.test.ts` 全绿。
- 提交：`feat: add headFollowShift helper for smooth follow-head panning`

## 阶段 C：main.ts 接线 + 全量测试
- 新增 `let tapeRenderCenter: number | null = null;`
- `renderTape`：增加 follow-head 动画分支 + 非跟随清理分支。
- `createMachine` / `resetMachine`：重置 `tapeRenderCenter = null`。
- 切视角 handler：重置 `tapeRenderCenter` 并清除 `#tape` 的 transform/transition。
- 跑 `tsc -b` + `vitest run` 全绿。
- 提交：`feat: smooth (animated) tape panning in follow-head view mode`

## 收尾
- 在内置浏览器（http://127.0.0.1:5173/）手动验收：运行/单步时纸带平滑滑动；
  切换/重置不跳变；随意拖动不受影响。
- 更新任务清单与工作日志。
