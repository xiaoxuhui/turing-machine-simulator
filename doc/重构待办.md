# 图灵机实验台重构待办

## P0：路线任务稳定性

- [x] 消除 `route-worker.ts` 的大数组展开。
- [x] Worker 失败后恢复按钮和状态。
- [x] 取消、完成、失败统一清理资源。

## P1：界面职责

- [x] 提取连续运行调度器。
- [x] 提取路线任务控制器。
- [ ] 将 `main.ts` 降至只负责装配和跨模块协调。

## P1：回归保护

- [x] 固化全部内置示例结果。
- [x] 覆盖路线大纸带和错误路径。
- [x] 完成真实浏览器交互测试。

## P2：后续版本

- [ ] 提取规则编辑器和瓷砖控制器。
- [ ] 统一并严格校验项目 JSON 格式。（纳入 v0.3.0）
- [ ] 支持非零初始读写头位置。
- [ ] 增加自动化浏览器端到端测试。

## v0.3.0 开源整改

### P0：发布阻断

- [ ] 消除执行日志用户字段 HTML 注入。
- [ ] 由仓库所有者选择并加入许可证。

### P1：可靠性与协作

- [ ] 项目 JSON 严格校验并原子导入。
- [ ] localStorage 失败可恢复。
- [ ] 隔离旧路线 Worker 的迟到事件。
- [ ] 增加 CI、贡献指南、安全政策、行为准则和协作模板。
- [ ] 同步 README、CHANGELOG 和包元数据。

### P2：质量改进

- [ ] 使用无偏、可测试的拼图洗牌。
- [ ] 修复按钮聚焦时空格快捷键重复触发。
- [ ] 评估规则符号“单 Unicode 字素簇”迁移方案。
- [ ] 评估超大路线任务的警告与预计耗时，不重新加入硬上限。
