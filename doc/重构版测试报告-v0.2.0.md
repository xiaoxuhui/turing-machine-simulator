# 图灵机实验台重构版测试报告 v0.2.0

## 测试结论

- 日期：2026-08-27
- 单元测试：24 项通过，0 项失败
- 测试文件：9 个
- 真实浏览器交互：I01–I06 通过
- 生产构建：通过
- 浏览器错误日志：0

## 自动化测试

| 模块 | 数量 | 结果 |
| --- | ---: | --- |
| 核心纸带、解析和机器 | 6 | 通过 |
| 内置示例 | 3 | 通过 |
| 执行调度器 | 2 | 通过 |
| 速度计算 | 2 | 通过 |
| 路线边界 | 2 | 通过 |
| 路线控制器 | 2 | 通过 |
| 路线视图 | 3 | 通过 |
| 瓷砖拼图 | 3 | 通过 |
| 项目草稿状态 | 1 | 通过 |

路线边界测试扫描 150,000 个位置，确认不再通过展开参数触发 `RangeError`。路线控制器测试覆盖 complete、Worker 消息 error 和意外 onerror 后的清理与恢复。

## 真实浏览器交互

| 编号 | 结果 | 验证内容 |
| --- | --- | --- |
| I01 | 通过 | 连续运行、停机、暂停控件保持响应 |
| I02 | 通过 | 目标 1000 步/秒运行，实际速度显示为真实测量值；测试机器 7 步停机时显示 402 步/秒 |
| I03 | 通过 | 路线生成完成，画布为 320×180 且非空，生成按钮恢复 |
| I04 | 通过 | 循环机器后台运行到 2,927,750 步时可取消，状态显示“已取消” |
| I05 | 通过 | Worker 运行期间主页面仍可交互；取消不改变模拟器运行状态 |
| I06 | 通过 | 390×844 窄屏无页面级横向溢出，控件与路线区域可见 |

测试临时使用循环规则 `q0,0 -> q0,0,N`，结束后已恢复原机器草稿。

## 兼容性

- 项目 JSON v1 未改变。
- localStorage 键未改变。
- 速度范围、实际速度、路线采样、瓷砖和快捷键保持兼容。
- 四个内置示例均有回归保护；回文示例已修正非回文误判。

## 已知剩余风险

- `main.ts` 仍包含规则编辑器、纸带视图、瓷砖和项目编解码等职责，后续应继续拆分。
- 项目 JSON 仍为扁平 v1，严格 schema 与迁移不属于本轮范围。
- 浏览器交互目前为人工驱动的真实浏览器验收，尚未落地仓库内 Playwright 自动化。
