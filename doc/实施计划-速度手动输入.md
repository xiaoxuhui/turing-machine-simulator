# 实施计划：目标速度手动输入

阶段 A（本提交）：规划文档三份。
阶段 B：新建 `src/speed-input.ts` + `tests/speed-input.test.ts`
  1. 导出 `SpeedSyncElements` / `applySpeed` / `attachSpeedSync`。
  2. 单测覆盖 T1–T5（双向同步、越界、空值、无循环）。
  3. 跑 `vitest run tests/speed-input.test.ts`。
阶段 C：`main.ts` + `index.html`(此处为 main.ts 内联模板) + `styles.css`
  1. HTML：`.speed` 内加 `<input id="speedNumber" type="number" min="1" max="1000" value="5">`。
  2. main.ts：引入模块；定义 `speedEls`；`updateSpeedText` 改用 `applySpeed`；`attachSpeedSync(speedEls)` 替换原滑杆监听。
  3. styles.css：数字框样式。
  4. 跑 `tsc -b` + 全量 `vitest run`，全绿后提交。

每阶段一 commit（docs / feat），提交后 push。
