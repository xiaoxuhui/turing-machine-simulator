# 实施计划：跟随读写头模式的滑动时长随速度自适应

## 阶段
1. **规划文档**（本目录三份 md）—— 已完成提交。
2. **core.ts 纯函数 `slideDurationMs` + 单测**
   - 在 `isHeadInWindow` / `headFollowShift` 附近新增 `slideDurationMs`。
   - `tests/core.test.ts` 补 T1–T6 用例。
   - 提交 `feat: add slideDurationMs helper for speed-adaptive panning`。
3. **main.ts 接线**
   - 导入 `slideDurationMs`。
   - `renderTape` 的 `follow-head` 分支：`transition = transform ${slideDurationMs(Number(value("speed")))}ms ease-out`。
   - 跑 `tsc -b` + 全量 `vitest`，提交 `feat: make follow-head slide duration adapt to speed`。

## 验收
- 全量 `vitest` 通过、`tsc` 类型检查干净。
- 内置浏览器：选「跟随读写头」→ 速度调 1 运行应连续缓行；速度调 50 运行应快速且流畅不积压。
