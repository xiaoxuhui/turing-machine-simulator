# 实施计划：平滑拖动

阶段 A（本提交）：规划文档三份。
阶段 B：改写 `src/tape-drag.ts`
  1. `TapeDragOptions` 增加 `applyShift`、`isEnabled`。
  2. `pointerdown`：`if (!options.isEnabled()) return;`
  3. `pointermove`：`setScroll(panFromDragDelta(...))` + `applyShift(remainder)`。
  4. `end`：`applyShift(0)` 后清除 dragging 类。
  5. 补 `tests/tape-drag.test.ts`（T1–T5），跑 vitest。
阶段 C：改写 `src/main.ts` + `src/styles.css`
  1. attachTapeDrag 增加 `applyShift`、`isEnabled` 回调。
  2. `setScroll` 加「同值不重渲染」早退。
  3. `tapeViewMode` change 清除 transform。
  4. styles.css 加 `will-change: transform`。
  5. 跑 `tsc -b` + 全量 `vitest run`，全绿后提交。

每阶段一 commit（docs / feat），提交后 push。
