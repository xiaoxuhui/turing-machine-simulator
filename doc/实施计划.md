# 图灵机实验台 v0.2.0 重构实施计划

## 目标

按《需求与测试用例》完成路线后台任务稳定化和 `main.ts` 第一轮职责拆分，保持现有行为兼容。

## 执行原则

- 先写测试和底层模块，再接入页面。
- 每个逻辑单元单独提交。
- 每阶段测试通过后再进入下一阶段。
- 不混入项目格式迁移或整页重写。

## 待办清单

### 阶段 0：规划与基线

- [x] 建立 v0.2.0 需求与测试用例。
- [x] 补充 v0.2.0 重构设计。
- [x] 记录实施计划和优先级。
- [x] 确认基线 15/15 单元测试通过。

### 阶段 1：路线任务稳定化

- [x] 增加无展开参数的纸带边界扫描。
- [x] 仅在采样时创建完整纸带快照。
- [x] 增加 Worker error 消息与主线程恢复路径。
- [x] 增加大纸带和错误恢复测试。

### 阶段 2：职责拆分

- [x] 提取连续运行调度器。
- [x] 提取路线 Worker 控制器。
- [x] 将 `main.ts` 改为装配控制器。
- [x] 保持速度、路线、取消和状态文案兼容。

### 阶段 3：回归与验收

- [x] 增加内置示例回归测试。
- [x] 运行全部单元测试和生产构建。
- [x] 在真实浏览器执行 I01–I06。
- [x] 更新重构测试报告、重构待办和阶段总结。

## 执行记录

- 单元测试：24/24 通过，9 个测试文件。
- 生产构建：TypeScript 与 Vite 构建通过。
- 浏览器：高速运行实际速度 402 步/秒（机器 7 步后停机）；路线生成完成、取消后恢复、生成期间继续响应均通过。
- 窄屏：390×844 视口无页面级横向溢出。
- 浏览器错误日志：0。
- 回归测试发现并修复回文示例将 `1011` 错判为接受的问题。

## 提交计划

1. `docs: establish v0.2.0 refactor plan`
2. `fix: harden route worker lifecycle`
3. `refactor: extract execution scheduler`
4. `refactor: extract route controller`
5. `test: cover built-in machine examples`
6. `docs: record v0.2.0 refactor verification`
