# 图灵机实验台 v0.3.0 阶段总结

## 已交付

- 安全执行日志：展示模型与 DOM 渲染边界从 `main.ts` 提取。
- 严格项目编解码：格式、字段、范围和规则语义通过后才替换工作台。
- 可恢复本地存储与旧 Worker 事件隔离。
- 无偏拼图洗牌和按钮空格冲突修复。
- CI、Issue/PR 模板、贡献指南、安全政策、行为准则、CHANGELOG 和包元数据。

## 验证

- 37/37 单元测试通过，TypeScript 与 Vite 生产构建通过。
- 真实浏览器验证安全日志、超长路线取消、短路线完成和桌面布局。
- 项目恢复默认示例，构建产物已更新但仍由 `.gitignore` 排除。

## 架构变化

- `execution-log.ts` 负责安全展示字段和文本 DOM。
- `project-codec.ts` 成为文件导入与缓存恢复的唯一校验边界。
- `RouteController` 清理指定 Worker 并拒绝旧实例事件。
- `shuffle.ts` 提供可注入随机源的纯 Fisher–Yates 实现。

## 发布状态

本轮没有推送、打标签或发布。`package.json` 已准备为 0.3.0，项目采用 MIT License，但 `CHANGELOG` 保持 Unreleased。

## 剩余工作

- [x] 仓库所有者选择并加入 MIT License。
- [x] 确认并修复 `origin/main [gone]` 的远端追踪关系。
- [ ] 决定是否重写包含公开作者邮箱的历史提交。
- [ ] 增加自动化浏览器 E2E 和窄屏截图回归。
- [ ] 继续拆分规则编辑器与瓷砖控制器，评估 Unicode 字素簇规则迁移。
