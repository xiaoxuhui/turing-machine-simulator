# 实施计划：纸带视角「随意拖动」模式

- **阶段A** 规划文档（本组三份）。
- **阶段B** `core.ts`：`TapeViewMode` 加 `"free-drag"`；`tapeWindowCenter` 处理该模式；新增 `effectiveTapeScroll` 纯函数；补 `core.test.ts` 用例。
- **阶段C** `main.ts`：视角下拉加「随意拖动」选项；`renderTape` 改用 `effectiveTapeScroll`；`setScroll` 门控；`change` 事件归零；纸带 `can-drag` 类切换。`styles.css` 光标调整。
- **阶段D** 全量 `vitest` + `tsc` 类型检查，确保通过；按 skill 每阶段提交。

## 验收
- 视角下拉出现「随意拖动」。
- 选「随意拖动」时可拖动纸带，初始在 0，窗口随之平移。
- 选「跟随读写头 / 固定在 0」时纸带不可拖动（光标正常），原跨模式拖动已删除。
