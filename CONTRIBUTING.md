# 贡献指南

感谢参与图灵机实验台。开始前请先搜索现有 Issue；行为变化或较大重构建议先提交功能建议，写明用户场景和验收标准。

## 开发流程

1. 使用 Node.js 20.19+ 和 pnpm 11，执行 `pnpm install --frozen-lockfile`。
2. 在 `doc/需求与测试用例.md` 和 `doc/实施计划.md` 记录可测行为；设计边界变化同步到设计文档。
3. 每个提交只包含一个逻辑功能，优先使用 `feat:`、`fix:`、`refactor:`、`test:`、`docs:` 或 `ci:` 前缀。
4. 为领域规则、回归缺陷和异步边界补 Vitest 测试。
5. 提交前运行 `pnpm run check`，界面变化还需在桌面与窄屏真实浏览器验证。

请保持项目 JSON v1、现有内置示例和无限正整数路线步数兼容。不要提交 `node_modules/`、`dist/`、本地日志、密钥或包含个人数据的项目文件。

## Pull Request

PR 应说明目的、兼容性影响、测试结果和剩余风险。界面改动请附桌面与移动截图；数据格式变更必须同时提供迁移与旧版兼容测试。
